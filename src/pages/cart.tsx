import Layout from "@/layout/Layout";
import React from "react";

type Props = {};

const cart = (props: Props) => {
  return (
    <Layout>
      <div>cart</div>
    </Layout>
  );
};

export default cart;
