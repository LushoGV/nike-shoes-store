export const ALL_PRODUCTS = "/api/product"
export const ONE_PRODUCT = (product:string) => `/api/product/${product}`

export const CATEGORIES_LIST = "/api/product/category/list"
export const ONE_CATEGORY = (category:string) => `/api/product/category/${category}`

export const SIZES = "/api/product/sizes"