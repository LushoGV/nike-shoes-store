import { HOME, SIGNUP } from "./ClientRoutes";

export const redirectToHome = {
  redirect: {
    destination: HOME,
  },
};

export const redirectToSIGNUP = {
  redirect: {
    destination: SIGNUP,
  },
};
